# Authentication   [Skip link to Authentication](https://docs.lumalabs.ai/docs/video-generation\#authentication)

1. Get a key from [https://lumalabs.ai/dream-machine/api/keys](https://lumalabs.ai/dream-machine/api/keys)
2. Use the key as Bearer token to call any of the API endpoints

```
Authorization: Bearer <luma_api_key>
```

# Models   [Skip link to Models](https://docs.lumalabs.ai/docs/video-generation\#models)

| name | model param |
| --- | --- |
| Ray 2 Flash | ray-flash-2 |
| Ray 2 | ray-2 |

# API Reference   [Skip link to API Reference](https://docs.lumalabs.ai/docs/video-generation\#api-reference)

[Open](https://docs.lumalabs.ai/reference/creategeneration)

# Ray 2 Text to Video   [Skip link to Ray 2 Text to Video](https://docs.lumalabs.ai/docs/video-generation\#ray-2-text-to-video)

sh

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "an old lady laughing underwater, wearing a scuba diving suit",
  "model": "ray-2",
  "resolution": "720p",
  "duration": "5s"
}
'
```

Resolution can be 540p, 720p, 1080, 4k

# Ray 2 Image to Video   [Skip link to Ray 2 Image to Video](https://docs.lumalabs.ai/docs/video-generation\#ray-2-image-to-video)

Shell

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "A tiger walking in snow",
  "model": "ray-2",
  "keyframes": {
    "frame0": {
      "type": "image",
      "url": "https://storage.cdn-luma.com/dream_machine/7e4fe07f-1dfd-4921-bc97-4bcf5adea39a/video_0_thumb.jpg"
    }
  }
}
'
```

# How to use concepts   [Skip link to How to use concepts](https://docs.lumalabs.ai/docs/video-generation\#how-to-use-concepts)

cURL

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "a car",
  "model": "ray-2",
  "resolution": "720p",
  "duration": "5s",
  "concepts": [\
  	{\
    	"key": "dolly_zoom"\
    }\
  ]
}
'
```

We have this new `/concepts/list` api one can hit to get list of concepts available

`https://api.lumalabs.ai/dream-machine/v1/generations/concepts/list`

# Text to Video   [Skip link to Text to Video](https://docs.lumalabs.ai/docs/video-generation\#text-to-video)

shell

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "an old lady laughing underwater, wearing a scuba diving suit",
  "model": "ray-2"
}
'
```

### Downloading a video   [Skip link to Downloading a video](https://docs.lumalabs.ai/docs/video-generation\#downloading-a-video)

Shell

```
curl -o video.mp4 https://example.com/video.mp4
```

## With loop, aspect ratio   [Skip link to With loop, aspect ratio](https://docs.lumalabs.ai/docs/video-generation\#with-loop-aspect-ratio)

Shell

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "an old lady laughing underwater, wearing a scuba diving suit",
  "model": "ray-2",
  "aspect_ratio": "16:9",
  "loop": true
}
'
```

# Image to Video   [Skip link to Image to Video](https://docs.lumalabs.ai/docs/video-generation\#image-to-video)

> ☁️
>
> ### Image URL   [Skip link to Image URL](https://docs.lumalabs.ai/docs/video-generation\#image-url)
>
> You should upload and use your own cdn image urls, currently this is the only way to pass an image

## With start frame   [Skip link to With start frame](https://docs.lumalabs.ai/docs/video-generation\#with-start-frame)

Shell

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "A tiger walking in snow",
  "model": "ray-2",
  "keyframes": {
    "frame0": {
      "type": "image",
      "url": "https://storage.cdn-luma.com/dream_machine/7e4fe07f-1dfd-4921-bc97-4bcf5adea39a/video_0_thumb.jpg"
    }
  }
}
'
```

## With start frame, loop, aspect ratio   [Skip link to With start frame, loop, aspect ratio](https://docs.lumalabs.ai/docs/video-generation\#with-start-frame-loop-aspect-ratio)

Shell

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "A tiger walking in snow",
  "model": "ray-2",
  "keyframes": {
    "frame0": {
      "type": "image",
      "url": "https://storage.cdn-luma.com/dream_machine/7e4fe07f-1dfd-4921-bc97-4bcf5adea39a/video_0_thumb.jpg"
    }
  },
  "loop": false,
  "aspect_ratio": "16:9"
}
'
```

## With ending frame   [Skip link to With ending frame](https://docs.lumalabs.ai/docs/video-generation\#with-ending-frame)

Shell

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "A tiger walking in snow",
  "model": "ray-2",
  "keyframes": {
    "frame1": {
      "type": "image",
      "url": "https://storage.cdn-luma.com/dream_machine/7e4fe07f-1dfd-4921-bc97-4bcf5adea39a/video_0_thumb.jpg"
    }
  },
  "loop": false,
  "aspect_ratio": "16:9"
}
'
```

## With start and end keyframes   [Skip link to With start and end keyframes](https://docs.lumalabs.ai/docs/video-generation\#with-start-and-end-keyframes)

Shell

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "A tiger walking in snow",
  "model": "ray-2",
  "keyframes": {
    "frame0": {
    	"type": "image",
      "url": "https://storage.cdn-luma.com/dream_machine/7e4fe07f-1dfd-4921-bc97-4bcf5adea39a/video_0_thumb.jpg",
    },
    "frame1": {
      "type": "image",
      "url": "https://storage.cdn-luma.com/dream_machine/7e4fe07f-1dfd-4921-bc97-4bcf5adea39a/video_0_thumb.jpg",
    }
  },
  "loop": false,
  "aspect_ratio": "16:9"
}
'
```

# Extend Video   [Skip link to Extend Video](https://docs.lumalabs.ai/docs/video-generation\#extend-video)

## Extend video   [Skip link to Extend video](https://docs.lumalabs.ai/docs/video-generation\#extend-video-1)

> Extend is currently supported only for generated videos. Please make sure the generation is in `completed` state before passing it

Shell

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "The tiger rolls around",
  "model": "ray-2",
  "keyframes": {
    "frame0": {
      "type": "generation",
      "id": "123e4567-e89b-12d3-a456-426614174000"
    }
  }
}
'
```

## Reverse extend video   [Skip link to Reverse extend video](https://docs.lumalabs.ai/docs/video-generation\#reverse-extend-video)

> Generate video leading up to the provided video.
>
> Extend is currently supported only for generated videos. Please make sure the generation is in `completed` state before passing it

Python

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "The tiger rolls around",
  "model": "ray-2",
  "keyframes": {
    "frame1": {
      "type": "generation",
      "id": "123e4567-e89b-12d3-a456-426614174000"
    }
  }
}
'
```

## Extend a video with an end-frame   [Skip link to Extend a video with an end-frame](https://docs.lumalabs.ai/docs/video-generation\#extend-a-video-with-an-end-frame)

> Extend is currently supported only for generated videos. Please make sure the generation is in `completed` state before passing it

Python

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "The tiger rolls around",
  "model": "ray-2",
  "keyframes": {
    "frame0": {
      "type": "generation",
      "id": "123e4567-e89b-12d3-a456-426614174000"
    },
    "frame1": {
      "type": "image",
      "url": "https://storage.cdn-luma.com/dream_machine/7e4fe07f-1dfd-4921-bc97-4bcf5adea39a/video_0_thumb.jpg"
    }
  }
}
'
```

## Reverse extend a video with a start-frame   [Skip link to Reverse extend a video with a start-frame](https://docs.lumalabs.ai/docs/video-generation\#reverse-extend-a-video-with-a-start-frame)

> Extend is currently supported only for generated videos. Please make sure the generation is in `completed` state before passing it

Python

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "The tiger rolls around",
  "model": "ray-2",
  "keyframes": {
    "frame0": {
      "type": "image",
      "url": "https://storage.cdn-luma.com/dream_machine/7e4fe07f-1dfd-4921-bc97-4bcf5adea39a/video_0_thumb.jpg"
    },
    "frame1": {
      "type": "generation",
      "id": "123e4567-e89b-12d3-a456-426614174000"
    }
  }
}
'
```

## Interpolate between 2 videos   [Skip link to Interpolate between 2 videos](https://docs.lumalabs.ai/docs/video-generation\#interpolate-between-2-videos)

> Interpolate is currently supported only for generated videos. Please make sure the generation is in `completed` state before passing it

Python

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "The tiger rolls around",
  "model": "ray-2",
  "keyframes": {
    "frame0": {
      "type": "generation",
      "id": "123e4567-e89b-12d3-a456-426614174000"
    },
    "frame1": {
      "type": "generation",
      "id": "123e4567-e89b-12d3-a456-426614174000"
    }
  }
}
'
```

# Generations   [Skip link to Generations](https://docs.lumalabs.ai/docs/video-generation\#generations)

## Get generation with id   [Skip link to Get generation with id](https://docs.lumalabs.ai/docs/video-generation\#get-generation-with-id)

Shell

```
curl --request GET \
     --url https://api.lumalabs.ai/dream-machine/v1/generations/123e4567-e89b-12d3-a456-426614174000 \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx'
```

## List all generations   [Skip link to List all generations](https://docs.lumalabs.ai/docs/video-generation\#list-all-generations)

Shell

```
curl --request GET \
     --url 'https://api.lumalabs.ai/dream-machine/v1/generations?limit=10&offset=10' \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx'
```

## Delete generation   [Skip link to Delete generation](https://docs.lumalabs.ai/docs/video-generation\#delete-generation)

Shell

```
curl --request DELETE \
     --url https://api.lumalabs.ai/dream-machine/v1/generations/123e4567-e89b-12d3-a456-426614174000 \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx'
```

# Camera Motions   [Skip link to Camera Motions](https://docs.lumalabs.ai/docs/video-generation\#camera-motions)

> 📘
>
> ### How to use camera motion   [Skip link to How to use camera motion](https://docs.lumalabs.ai/docs/video-generation\#how-to-use-camera-motion)
>
> Just add the camera motion value as part of prompt itself

## Get all supported camera motions   [Skip link to Get all supported camera motions](https://docs.lumalabs.ai/docs/video-generation\#get-all-supported-camera-motions)

Shell

```
curl --request GET \
     --url https://api.lumalabs.ai/dream-machine/v1/generations/camera_motion/list \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx'
```

## How to use camera motion   [Skip link to How to use camera motion](https://docs.lumalabs.ai/docs/video-generation\#how-to-use-camera-motion-1)

Camera is controlled by language in Dream Machine. You can find supported camera moves by calling the Camera Motions endpoint. This will return an array of supported camera motion strings (like "camera orbit left") which can be used in prompts. In addition to these exact strings, syntactically similar phrases also work, though there can be mismatches sometimes.

# Example Response   [Skip link to Example Response](https://docs.lumalabs.ai/docs/video-generation\#example-response)

Shell

```
{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "state": "completed",
  "failure_reason": null,
  "created_at": "2023-06-01T12:00:00Z",
  "assets": {
    "video": "https://example.com/video.mp4"
  },
  "version": "ray-2",
  "request": {
    "prompt": "A serene lake surrounded by mountains at sunset",
    "aspect_ratio": "16:9",
    "loop": true,
    "keyframes": {
      "frame0": {
        "type": "image",
        "url": "https://example.com/image.jpg"
      },
      "frame1": {
        "type": "generation",
        "id": "123e4567-e89b-12d3-a456-426614174000"
      }
    }
  }
}
```

# How to get a callback when generation has an update   [Skip link to How to get a callback when generation has an update](https://docs.lumalabs.ai/docs/video-generation\#how-to-get-a-callback-when-generation-has-an-update)

- It will get status updates (dreaming/completed/failed)
- It will also get the video url as part of it when completed
- It's a `POST` endpoint you can pass, and request body will have the generation [object](https://github.com/lumalabs/lumaai-api/blob/35768db34e52600e319cb799e4488249e61eef17/openapi.yaml#L258) in it
- It expected to be called multiple times for a status
- If the endpoint returns a status code other than 200, it will be retried max 3 times with 100ms delay and the request has a 5s timeout

example

Shell

```
curl --request POST \
     --url https://api.lumalabs.ai/dream-machine/v1/generations \
     --header 'accept: application/json' \
     --header 'authorization: Bearer luma-xxxx' \
     --header 'content-type: application/json' \
     --data '
{
  "prompt": "an old lady laughing underwater, wearing a scuba diving suit",
  "callback_url": "<your_api_endpoint_here>"
}
'
```

Updated3 months ago

* * *

Ask AI